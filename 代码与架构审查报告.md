# 考研学习链 · 全量代码与架构审查报告

> 审查范围：全仓库 118 个文件（26 个 Python 模块 + 3 个 bat/shell 入口 + 1 个 CI 工作流 + 协议/状态/文档）
> 审查方法：逐文件精读 + 实证复现（py_compile 全量通过；对 2 处高危缺陷做了运行时验证）
> 结论速览：**P0 级缺陷 5 项，P1 级 12 项，P2/P3 级 12 项**；三项核心设计中，私教协议"能跑通但未收敛"，外置状态机"有骨架缺校验"，自测看板"本地优秀、云端失效"。

---

## 一、程序正确性（Correctness）

### 高严重度（P0）

| # | 位置 | 问题 | 依据 |
|---|---|---|---|
| C-1 | `tools/agent/tools_impl.py:124`（read_file）**与** `:493`（read_exam_paper） | 调用 `pdf_extractor.extract_pdf_pages(path, max_pages=N)`，但 `tools/skills/pdf_extractor.py` 只定义了单页版 `extract_pdf_page(pdf_path, page_num=1)`，全仓库无 `extract_pdf_pages` 定义 | 实测 `hasattr(pdf_extractor,'extract_pdf_pages') == False`；全仓 grep 仅 2 处调用、0 处定义。异常被 `execute_tool` 的 try 吞成字符串 ⇒ 表面不崩，**"从真题 PDF 抽题"这一 README 头号卖点 100% 不可用** |
| C-2 | `tools/agent/tools_impl.py:578-585`（log_mistake 工具） | 传参为 `mistake_type/card_content/expert_prescription`，而 `tools/skills/error_logger.py:32` 签名为 `(subject, title, error_type, detail, prescription, question)` | 实测抛 `TypeError: unexpected keyword argument 'mistake_type'` ⇒ **Agent 自动归档错题永远失败**；连带 `hooks.py:131` 的 PostToolUse 联动靠 `"Success" in tool_result` 判定，永不触发 |
| C-3 | `tools/ky_cli.py:1866-1880`（网关 /v1/chat/completions） | `req_data` 在 `except` 分支未定义，但 1880 行 `req_data.get("model", ...)` 仍引用它 | 非 JSON 的 POST body（部分群机器人/探针请求）⇒ **NameError ⇒ HTTP 500**，微信 ClawBot 链路首当其冲 |
| C-4 | `tools/ky_cli.py:1287-1289` 与 `:1540-1544` | `/exit`、`/quit` 在 1287 分支先命中并 `break`；1540 分支（含 `agent_runner.hooks.trigger_session_end`）是**不可达死代码** | 顺序执行即可判定。**会话结束钩子永不触发**，设计中的"学习进度与任务落盘"缺失 |
| C-5 | `tools/ky_cli.py:2028 / 2099` | 使用单线程 `HTTPServer`，且绑定 `0.0.0.0:8088`，`/v1/chat/completions` 与 `/api/ask` **无任何鉴权** | 一次 60s 的 LLM 请求会阻塞所有 `/api/live` 轮询与并发提问（Web 伴侣假死）；局域网内任何人可白嫖你的 API Key 额度。且 `run_repl` 在 `ky` 启动时**静默**拉起该服务，用户无感知 |

### 中严重度（P1）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| C-6 | `tools/skills/error_logger.py:140-163` | `get_due_reviews` 注释声明按 1/3/7/15 天到期，代码却**把所有未掌握题全部返回**，无任何日期门控 | 实测 7 条待复测题全部"到期"。艾宾浩斯退化为随机抽题，当天会反复抽同一题 |
| C-7 | `tools/skills/error_logger.py:208-218` | 正则未命中时走 fallback：把 `[已掌握]` 追加到**标题旁**，而 `scan_error_records:103` 只读「掌握状态」行 | 复测通过 → 状态仍为 `待复测` → **永久留在待测队列** |
| C-8 | `error_logger.py:211`（`count=1`） | 同名标题只更新第一条 | 实测存在两条 `泰勒展开阶数匹配失误`，另一条永远无法出库 |
| C-9 | `tools/skills/math_verifier.py:184` 与 `:128` | `oo if "inf" in dest_str/ "oo" in b_str` ⇒ `-inf`、`-oo` 被判成**正无穷** | `limit 1/x as x->-inf` 结果错误，与"100% 精确"承诺相悖 |
| C-10 | `math_verifier.py:106-107` | `all(m > 0 for m in minors if ... m.is_real)`：符号矩阵的 `is_real` 为 `None` ⇒ 元素被过滤 ⇒ 空序列 `all()` 返回 `True` | 含参数的二次型被**误判为正定** |
| C-11 | `tools/ky_cli.py:487-491` | `int(target_id)` 写在 `try` 之外 | 非数字 QQ 号直接抛 `ValueError` 未捕获，`/notify` 崩溃 |
| C-12 | `tools/agent/context_engine.py:136-161` | 压缩时"保留最后 6 条"可能在 `assistant(tool_calls)` 与其 `tool` 结果之间切断 | 产生孤儿 tool 消息，OpenAI 兼容接口返回 400，且发生在长对话后最难排查的时刻 |
| C-13 | `tools/agent/mcp_client.py:99-118` | `_send_request(timeout=15)` 的 `timeout` **从未使用**，`readline()` 可能永久阻塞；`MCPClientManager.close_all()` 无任何调用者 | MCP Server 无响应 ⇒ 整个 Agent 卡死；子进程泄漏 |
| C-14 | `tools/agent/hooks.py:109-124` | 考纲红线仅判断 `subj == "math"`，不读 `math_key` | 用户切到**数学一**后，合法的"三重积分/曲面积分/无穷级数"被硬拦截（数一必考内容） |
| C-15 | `tools/study_planner.py:815-873` | `generate_plan_and_today_files` **无条件覆盖**四科 `_状态/今日任务.md` | 中途重跑 `/plan` 会清空当天完成勾选与 AI 生成的个性化任务（对比：`init_workspace.copy_templates` 有 `if not target.exists()` 保护，两处策略不一致） |
| C-16 | `.gitignore:36-43` + `.github/workflows/deploy-pages.yml:38-40` + `05-考研看板/build.py:107-120` | 真实 `_状态/*.md` 被 gitignore；CI 里 `build.py` 只能读到 `*.template.md` 并以模板内容重建 `docs/index.html` | **Pages 每次自动部署都会把真实数据覆盖成模板空数据**，SETUP.md 承诺的"手机端约 1 分钟同步最新掌握度"实际不成立 |
| C-17 | `05-考研看板/build.py:107-120` | `read()` 回退到 `.template.md` 时**无任何告警** | 用户全程无感知，只会觉得"看板数据不对" |

### 低严重度（P2）

| # | 位置 | 问题 |
|---|---|---|
| C-18 | `ky_cli.py:1045` | `print_today_tasks_summary` 用 `read_text(encoding="utf-8")`，未使用同文件已有的 `read_text_safe`（三编码回退）⇒ GBK 状态文件 `UnicodeDecodeError` 崩溃 |
| C-19 | `ky_cli.py:992` / `build.py:21-22` / `ky_config.json:5` | 三处倒计时口径不一致：`print_welcome` 硬编码 12-19、build.py 有 12-19 与 12-20 两个常量、配置里 `days_left=107` 是静态值（今天应 106） |
| C-20 | `ky_cli.py:36` | `HISTORY_FILE` 定义后**从未使用**（死代码）；CLI 会话历史退出即丢 |
| C-21 | `05-考研看板/tools/append_hook.py:34-39` | TARGETS 用相对路径（依赖 CWD），且第 4 项 `04-专业课\[你的目标院校]\学习进度\AGENTS.md` 与真实结构不符 ⇒ 必定 MISS；注入文本指向旧路径 `C:\Users\<username>\Desktop\考研看板\auto-update.bat`（实际在 `05-考研看板\`） |
| C-22 | `tools/agent/tools_impl.py:229-231` | `list_directory` 在 `depth == max_depth` 时 `continue`，该层文件被整体跳过 |
| C-23 | `tools/agent/tools_impl.py:448-453` | `web_search` 抓取 DuckDuckGo HTML，标题取 `result__url` 类（实为 URL 而非标题），无选择器变更容错 |
| C-24 | `ky`（bash 入口） | 使用 `python3`，Windows 上通常只有 `python` ⇒ Git Bash 下 `./ky` 大概率报 command not found；`ky.bat:7` 与 `更新看板.bat:8` 硬编码 `D:\Python\Python312\python.exe` |
| C-25 | `04-专业课/学情档案.md` 与 `04-专业课/_状态/学情档案.md` | 两份内容完全一致（均 1183 字节），而所有代码只读前者（`ky_cli.py:276`、`context_engine.py:60`、`build.py:93-96`）⇒ 后者是必然分歧的僵尸副本 |

### 安全专项（P1）

| 位置 | 问题 |
|---|---|
| `tools/agent/sandbox.py:96-106` | 工作区外**任何已存在**的 `.md/.txt/.json/.pdf/.docx/.csv` 文件都放行读取 ⇒ Agent 可读全盘笔记与其它项目数据 |
| `sandbox.py:38-48` | 命令黑名单可绕过：`Remove-Item -Recurse -Force`、`rm -rf C:\...`、`curl x \| sh` 均不在规则内 |
| `permissions.py:66` | `--permission=auto` 自动放行 Level 3（Shell 执行），叠加上面两点 ⇒ 全自动模式下的安全网仅剩可绕过的黑名单 |
| `.gitignore` 未忽略 `.memory/` | `.memory/session.md`、`decisions.md` 记录学员决策与当日攻坚内容，一旦 `git init && git push` 就会随代码公开（与 SETUP.md "隐私隔离"承诺冲突） |

---

## 二、实现可行性（三项核心设计 vs 实际代码）

### 2.1 AI Agent 私人教师协议 —— ⭐⭐⭐☆☆（骨架完整，未收敛到可执行）

**已实现**：根 `AGENTS.md`（149 行）+ 4 科 `AGENTS.md` + CLI/ContextEngine 双层系统提示装配 + 参考资料白名单物理核验 + 反虚构红线（无资料时注入"严禁虚构题源"硬约束）+ 4 种辅导风格定义。**装配链路是真跑通的**，实测四科 prompt 均 >200 字符且成功注入顶层协议。

**差距**：
1. **协议与 CLI 不对齐**（高）：根 `AGENTS.md` 第 2 节定义了口令映射表（`数学报到`/`英语报到`/`交作业`/`查漏`/`复盘`/`更新看板`），但 `ky_cli.py` 的斜杠分支只有英文指令；中文口令**没有任何代码处理**，在降级路径（无 AgentRunner 时）会被当作普通提问原样发给模型，"查漏""更新看板"两个口令更是连 LLM 侧都没有对应工具。
2. **辅导风格不可切换**（中）：定义了 4 种风格，`study_planner` 会把风格名写进 `AGENTS.md` 文本，`print_welcome:1007` 靠正则从 markdown 里抠出来展示——**但没有任何 `/style` 命令或配置项切换入口**，用户只能手改 Markdown，或重跑一遍 7 步向导。
3. **各科协议过薄**（中）：4 科 `AGENTS.md` 分别仅 21~23 行，缺少可执行的批改细则（采分点模板、错因五分类判定标准、步骤分分配规则）。这些规则实际散落在 `build_vision_prompt`、`run_repl` 的若干 prompt 片段里 ⇒ 输出稳定性强依赖模型，换模型即漂移。
4. **协议未对 AI 的行为做校验**（中）：没有任何机制检查"AI 是否真的写了状态文件""是否按五分类归因"，写不写全凭模型自觉。

### 2.2 外置状态机驱动 —— ⭐⭐⭐☆☆（有骨架，缺 schema 与闭环）

**已实现**：5 类状态文件（今日任务/学员档案/薄弱点雷达/学情档案/考试大纲）+ `.template.md` 回退 + 三处消费者（`ky_cli.build_system_prompt`、`context_engine`、`build.py`）+ 错题本艾宾浩斯标记。数据流是通的。

**差距**：
1. **无 schema，按中文标题+列号取值**（高）：`build.py:60-98` 的 `SECTIONS` 靠中文关键词（"模块掌握度雷达""错因五分类"）定位章节、靠硬编码列号 `{"label":1,"value":4}` 取数据。**标题改一个字，卡片静默消失**，且无任何警告。
2. **状态写回完全依赖 LLM 自觉**（高）：没有 `ky done`、`ky status update` 之类的直接写状态命令；打卡、改掌握度、记错因全部靠模型自己调 `write_file`/`log_mistake`——而 `log_mistake` 目前是坏的（C-2）。
3. **存在无人消费的死状态**（中）：`_状态/当前进度.md` 被 `verify_health.py` 列为必选，但 `build.py`、`ky_cli`、`context_engine` **都不读它**。
4. **艾宾浩斯未实现**（中）：见 C-6/C-7/C-8。
5. **session 记忆无限膨胀**（中）：`memory.append_memory` 无条数上限，`session.md` 会随会话线性增长并原样注入 system prompt。

### 2.3 自动化自测看板 —— ⭐⭐⭐⭐☆（本地很扎实）／⭐⭐☆☆☆（云端目标未达成）

**已实现且质量最高**：`build.py`（1210 行）实现了表格解析（保护 `$...$` 内竖线）、LaTeX 剥离、翻卡/遮罩自测、KaTeX 渲染、进度条目标刻度、localStorage 记忆页签、明暗主题、PWA meta、CI 友好。本地 `python build.py` 是可用的。

**差距**：
1. **云端自动同步被 CI 反向覆盖**（高，C-16）：隐私保护（gitignore 状态文件）与自动部署（CI 重建）直接冲突，最终 Pages 呈现模板数据。这是"理想设计 vs 实现"最典型的一处断裂。
2. **"离线 KaTeX 渲染"不成立**（中）：`docs/index.html:10/297-298` 三个资源全部来自 `cdn.jsdelivr.net`，断网即无公式排版，与 `SETUP.md:90` 表述不符。
3. **收工自动化链路断在最后一环**（中）：`append_hook.py` 想让 AI 每次收工自动跑 `auto-update.bat`，但目标路径写错且第 4 项永远 MISS（C-21）⇒ 实际仍需人工双击 `更新看板.bat`。
4. **无构建结果校验**（低）：`build.py` 只 print 文件大小，不校验"今日任务是否为空""解析出几张卡片"。

---

## 三、易用性提升（Usability）

| 类别 | 问题 | 落地建议 |
|---|---|---|
| **命令体系** | 三套入口并存（`ky <sub>`、`/xx` 斜杠指令、中文口令），`ky --help` 只列 8 条而交互内实际有 30+ 指令；`help` 文本与 `print_command_palette` 内容不一致 | 用 `argparse` 子命令统一，斜杠指令映射到同一 handler；`ky help <cmd>` 输出详情；中文口令走统一别名表 |
| **参数** | `--permission=` 只在解析首参时处理，`-p=` 是重复别名；`ky serve` 的端口必须是 `args[1]`（顺序敏感）；缺少 `--json`/`--quiet` | 全部迁移 argparse；为 `today/status/review` 增加 `--json`，便于脚本化与其它 Agent 调用 |
| **退出码** | 除 `update_dashboard.py` 外，失败几乎都只 `print` 不 `exit(non-zero)` | 统一错误退出码；`build.py` 在"今日任务为空/卡片数为 0"时给出 warning 码 |
| **输出可读性** | 盒子边框按 ASCII 宽度计算（`ky_cli.py:1019-1028`、`1156-1158`、`1075-1093`），CJK 下右侧竖线错位；spinner 用固定 48/52 个空格清行 | 用 `wcwidth` 思路自行计算显示宽度（零依赖可内置小表）；或改用不依赖对齐的缩进式排版 |
| **交互陷阱** | 数字 `1~5` 被占为快捷键，学员输入纯数字（如选择题答案 "2"）会被劫持；`/review` 进行中切到 `/math`，`active_quiz_item` 未清空 ⇒ 新回答被当作上一科错题的复测答案 | 快捷键仅在"上一轮有 AI 回复"时启用，并支持 `/esc` 退出；切换学科时 `active_quiz_item = None` |
| **首次体验** | 配置 API Key 必须进入多级菜单，无法 `ky config --set api_key=sk-...`；无 `ky doctor` 一键体检 | 增加非交互设置项与 `ky doctor`（整合 `verify_health.py` + 依赖检测 + 端口/Key 连通性自检） |
| **Windows 入口** | `ky`（bash）调用 `python3`，Windows Git Bash 下大概率失败；`.bat` 硬编码 `D:\Python\Python312` | `ky` 内做 `command -v python3 || command -v python` 回退；`.bat` 改为探测常见安装路径 |
| **文档** | README 声称"108 项全自动化测试"，实测 `assert_true` 110 处，但 `log_mistake`/`read_exam_paper` 只测了"是否在工具清单里"，**从未真正执行**（`test_ky_suite.py:557` 只查名字、`:595-601` 用假字符串触发钩子）⇒ 两个坏掉的旗舰工具在绿灯下溜过；SETUP.md "离线 KaTeX"不成立 | 补充"执行级"用例：真实调用每个 tool 并断言非 `ExecutionError`/`Error` 前缀；修正 SETUP 表述；README 补"已知限制"章节 |

---

## 四、后续改进方向（按优先级）

### P0 —— 阻断性缺陷，建议当天修复

1. **修复两个坏掉的 Agent 工具**：为 `pdf_extractor` 补齐 `extract_pdf_pages(path, max_pages)`（内部复用 `extract_pdf_page`），并把 `tools_impl.log_mistake` 的实参对齐 `error_logger.log_error_record` 的形参（最小改动：改调用处关键字；更稳：给 `log_error_record` 增加 `**kwargs` 别名兼容）。**收益**：README 头号卖点（真题 PDF 抽题）与核心闭环（错题自动归档）从 0% 变可用。
2. **给测试补"执行级"断言**：断言真实调用 `read_exam_paper`/`log_mistake`/`review_mistakes` 后返回值不以 `Error`/`ExecutionError`/`TypeError` 开头。**收益**：防止同类签名漂移再次绿灯蒙混。
3. **网关健壮性**：`req_data` 初始化为 `{}`；改用 `ThreadingHTTPServer`；默认绑定 `127.0.0.1`，对外监听需显式 `--host 0.0.0.0` 并支持 `KY_GATEWAY_TOKEN` 鉴权。**收益**：消除 500、消除伴侣假死、堵住局域网 API Key 白嫖。
4. **修复 `/exit` 死代码**：删除 `ky_cli.py:1287-1289` 的提前分支，让 1540 分支统一处理并触发 `SessionEnd`。**收益**：设计中的"会话收尾落盘"真正生效。
5. **`.gitignore` 补 `.memory/`**。**收益**：堵住当前唯一真实的隐私外泄通道。

### P1 —— 影响核心体验，建议 1 周内

6. **艾宾浩斯真正到期制**：错题卡增加 `下次复测日` 字段，`get_due_reviews` 按 `next_due <= today` 过滤；未通过则按 [1,3,7,15] 递推而非重置。**收益**：从"随机抽题"恢复为真正的间隔重复。
7. **修复错题状态回写**：`mark_error_status` 匹配失败时不要静默 fallback 到标题追加，改为返回 `(False, 原因)` 并由调用方提示；同名题改为按 `日期+标题` 双键定位。**收益**：杜绝"通过了却永远复测"。
8. **修 `math_verifier` 两处判定**：`-inf` 解析（先判负号再判 inf）、正定性 `is_real is None` 时保守返回"无法判定"。**收益**：保住"高精度验算"的可信度。
9. **重构看板发布链路**（二选一）：(a) 改为"本地 build + 直接发布 `docs/`"（CI 不再 rebuild）；(b) 新增 `状态快照.json` 随仓库提交、CI 从快照构建。**收益**：让"手机端 1 分钟同步"名副其实。**注意 (b) 会削弱隐私保护，需做成显式开关并脱敏**。
10. **状态文件 schema 化**：用带稳定 ID 的表头（如 `| id | 模块 | 掌握度 |`）替代中文章节名+列号，`build.py` 按 ID 取列并在解析不到时打印告警。**收益**：改文案不再静默丢数据。
11. **修钩子与边界**：考纲红线读 `math_key` 区分数一/二/三；`compact_context` 保证 tool_calls 与 tool 结果成对保留；`send_to_qq` 的 `int()` 移入 try；MCP 实现真实 readline 超时并注册 `atexit` 关闭子进程。
12. **清理僵尸资产**：删除 `04-专业课/_状态/学情档案.md` 副本；修正 `append_hook.py` 的目标路径与实际 `auto-update.bat` 位置；`_状态/当前进度.md` 要么接入看板要么从健康检查和模板中移除。
13. **保护今日任务不被覆盖**：`generate_plan_and_today_files` 写 `今日任务.md` 前先备份（或仅当文件不存在/日期非今天时才写）。

### P2 —— 长期体验与工程化，1 个月内

14. **CLI 重构为 argparse 子命令体系**，统一三套入口，补齐 `ky style`（辅导风格切换）、`ky done <任务>`（打卡写回状态）、`ky review`（艾宾浩斯）等直接操作状态的命令 —— 让状态机不再只依赖 LLM 自觉。
15. **中文口令落地**：`数学报到`/`交作业`/`查漏`/`更新看板` 在 CLI 层显式识别并路由到对应能力，实现 `AGENTS.md` 里承诺的协议。
16. **输出工程化**：CJK 宽度对齐；`--json` 输出模式；统一退出码；`ky doctor` 一键体检。
17. **文档校正**：README 补"已知限制"；SETUP 修正离线 KaTeX 说法与 Windows `ky` 入口说明；`AGENTS.md`/配置中的静态倒计时改为运行期计算。
18. **健壮性收尾**：`print_today_tasks_summary` 等处统一改用 `read_text_safe`；`list_directory` 深度边界修正；`session.md` 追加条数上限 + 定期归档。

---

## 附：实证复现记录

```
$ python -m py_compile tools/**/*.py 05-考研看板/build.py ...   → ALL_COMPILE_OK（无语法错误）

$ python -c "from skills import pdf_extractor"
  has extract_pdf_pages: False        # ← C-1
  has extract_pdf_page: True

$ python -c "error_logger.log_error_record(subject='math', title='t', mistake_type=..., card_content=...)"
  TypeError: log_error_record() got an unexpected keyword argument 'mistake_type'   # ← C-2

$ python -c "scan_error_records('math'); get_due_reviews('math')"
  math error records: 7
  due reviews: 5  (days_ago=1 也全部判为到期)                    # ← C-6
  泰勒展开阶数匹配失误 / 泰勒展开阶数匹配失误  (同名两条)          # ← C-8

$ git ls-files → exit 128（本地尚未初始化仓库）
  ⇒ C-16、.memory 泄露风险为"一旦 git init && push 即触发"的潜在问题
```
