# Python
__pycache__/
*.py[cod]
*.pyc_probe
*$py.class
.pytest_cache/
.playwright-cli/
.pytest_tmp/
.cargo_target/
dist_wheels/
scripts/gui_shots/
*.so
.Python
env/
build/
develop-eggs/
# [C7 修复] 锚定为**仓库根**的构建产物目录。原写法 `dist/` 会匹配任意层级的
# dist 目录，把 docs/assets/vendor/katex/<ver>/dist/（KaTeX 的 npm 包内部结构，
# 里面正是 katex.min.css/js 与字体）一并忽略掉 —— 于是"本地 vendor 随仓库分发"
# 静默失效，断网时公式照样退化成 LaTeX 源码串。改为根锚定后互不干扰。
/dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
.env
.env.*
config.json
ky_config.json
# [P1-4 修复] load_config() 读坏 ky_config.json 时会生成含明文 api_key 的备份，
# 此前未被忽略，一旦 `git add -A` 即随仓库泄露。
ky_config.corrupted.bak
*.corrupted.bak
ky_history.json

# 配置守卫快照目录（tools/config_guard.py）：内含含真实 api_key 的完整配置副本。
# [2026-09-21 加固] 该目录已**移出仓库根** —— 新写入去 config_guard.default_backup_dir()
# （Windows: %LOCALAPPDATA%/kaoyan-study-chain/config_backup，可用环境变量
# KY_CONFIG_BACKUP_DIR 覆盖），从源头不进任何文件系统遍历范围。
# 本行保留作为**纵深防御**：旧的历史快照目录（读取仍兼容）或用户把
# KY_CONFIG_BACKUP_DIR 指回仓库内时，git 层仍不会入库。
.config_backup/

# 内部开发审查与优化方案报告 (不推送到远端)
考研学习链_代码审查与改进报告*.md
README优化方案*.md
新功能技术方案*.md
*_extracted.txt

# ==========================================
# 隐私防泄露铁律 (Privacy Guardrails)
# 真实个人学情状态、做题记录、作业与错题保留在本地，不上传云端
# ==========================================

# 真实总战役规划：由 ky plan 本地生成，含个人科目组合与分数目标。
# 仓库仅保留脱敏模板 00_考研全科总战役规划.example.md
/00_考研全科总战役规划.md

# 内层看板产物目录：docs/（仓库根）是唯一发布源，这里是构建产物副本。
# 保留本地产出以便单机工具与门禁校验，但不入库 —— 两份 md5 完全相同的副本
# 曾占仓库体积的一半（各约 2.8MB）。
05-考研看板/docs/

# [C7 修复·默认离线] 第三方前端资源（KaTeX / marked 及 woff2 字体）现随仓库分发：
# 看板默认走本地 vendor（断网也能渲染公式），克隆即用、无需先联网构建一次。
# 体积约 740KB，换取「手机端在破网环境下公式不再退化成 LaTeX 源码串」。
# 如确需回到 CDN：设 KY_VENDOR_MODE=cdn 或运行 build.py --cdn。
!docs/assets/vendor/
docs/assets/vendor/**/*.js.map
05-考研看板/docs/assets/vendor/

# 个人界面主题配置（tokens 见 tools/theme/）：由用户在本机定制，
# 纯外观偏好、不随仓库分发；缺失时自动使用内置预设。
/ui_theme.json

**/_状态/*.md
!**/_状态/*.template.md
!**/_状态/*.example.md
!**/*核心速记*.md

**/学情档案.md
!**/学情档案.template.md
!**/学情档案.example.md

# 个人每日笔记与作业
**/每日笔记/*.md
!**/每日笔记/_模板.md
!**/每日笔记/README.md

**/每日作业/*.md
!**/每日作业/_模板.md
!**/每日作业/README.md

# 个人错题本
**/错题本/*.md
!**/错题本/_模板.md
!**/错题本/_索引.md
!**/错题本/README.md
*.keys.json
.*.keys.json

**/错题与长难句本/*.md
!**/错题与长难句本/_模板.md
!**/错题与长难句本/_索引.md
!**/错题与长难句本/README.md

# 个人参考资料库 (大体积课件与教材 PDF 本地存放，不上传)
**/参考资料/*
!**/参考资料/README.md

# Large binary files & PDFs (Keep local for copyright & storage)
*.pdf
*.zip
*.tar.gz
*.rar
*.7z

# OCR & Temporary Scratch Images
scratch/
scratch_*.png
scratch_*.jpg
temp_*.png
temp_*.jpg
*.tmp
.tmp.driveupload/
*.docx
考研学习链_功能规划与竞品分析报告*.txt

# Operating System Files
.DS_Store
Thumbs.db
desktop.ini

# IDE & Editor files
.idea/
.vscode/
*.swp
*.swo
*~
.workbuddy/
.claude/
.agents/

# 全量自检沙箱：每轮自检会在这里铺开成百上千个临时工作区副本，
# 其中包含各份 ky_config.json / _状态 快照，属本机调试残留，绝不上传。
.codex_full_check_*/

# 三级分层记忆 (学员个人学情与决策，本地独占)
.memory/

# Plan Mode 快照备份沙箱 (本地独占，不上传云端)
.checkpoint/

# Rust / Cargo 构建目录与本地动态库 (源码保留在 rust_ext/src，二进制产物不入库)
target/
**/target/
rust_ext/target/
*.pyd
*.pdb

# 临时分析报告、竞品方案与搜索规划文件
*审查报告*.md
*路线图*.md
# 外部评审/改造方案文档（含内部短板实测，不随仓库公开；C6 推前发现）
*评审*.md
/state_snapshot.json
!docs/state_snapshot.json
!05-考研看板/docs/state_snapshot.json
*.log
/logs/
logs/

# ── 学员隐私：目标院校/专业社媒档案（绝不入库、绝不发布）──
docs/experiences/
!docs/experiences/README.md
.memory/experiences/
04-专业课/双校考情对比_*.md
04-专业课/目标院校情报_*.md
# D 盘工作区保留规则：规划类草稿不入库
*规划.txt

public_release/
test_out.txt
simulation_report.md
*考纲变动分析*.md
*考试大纲_backup*.md
*演示样例*.md

# Global Backup & Test Artifacts
*_backup_*.md
*测试*.md
*演示样例*.md

# ── 开源发布整理（2026-09-14）──
# sync_publish 导出副本的标记文件（工具生成，副本仓库不提交；C6 推前发现）
.sync_publish_marker
# 个人本地备份（脱敏前的真实配置，仅本机保留）
*.local.md
# 内部审查/调研文档（含本机路径，不随仓库公开）
FSRS重构审查与修正报告.md
kaoyan_chain_github_调研.md
CODE_REVIEW_*.md
.sim_*.txt
.sim_review_*.txt
simulation_cli.json
.dashboard_check_*.js
.llm_*.txt
.atomic_test.log
.httpd.log
.lint_out.txt
.pytest_out.txt
x.js
# 历史遗留开发脚本（含本机路径，不公开）
scripts/legacy/

# ── 报考意向脱敏（2026-09-16）──
# 自命题年份考纲文件名含真实报考院校名（如「2027考纲_814信号与系统.md」，
# 814 为某校自命题科目代码），公开即泄露报考意向 —— 仅本地保留，不入库。
# 通配只匹配「年份考纲_」前缀的生成文件，不影响随仓库公开的「考试大纲.md」。
/04-专业课/20*考纲_*.md

# ── 全国高校库：原始快照与抓取中间产物不入库（2026-09-19）──
# 只提交派生库 data/universities/national_institutions.json（可由快照复现）。
# 原始快照共约 3.5MB，且其中 fjw345/zsts119 两个上游仓库**未声明任何许可**，
# 原样提交到公开仓库属再分发他人未授权汇编数据 —— 故仅本机留存。
data/universities/_sources/
# exam_subjects.json 是 _sources/chsi_408_offerings.json 的消费者视图（1.8MB），
# 408 科目已内嵌进 national_institutions.json 的 departments，本地留存即可。
data/universities/exam_subjects.json
# 抓取过程的临时落盘目录
.tmp_unidata/

# ── 检索知识库：运行时产物，不入库（2026-09-21）──
# data/knowledge/embeddings.db 由 KnowledgeStore 首次使用/切片入库时自动创建
# （sqlite 单文件，可由「切片入库」重建），属本机状态而非源码。
data/knowledge/
